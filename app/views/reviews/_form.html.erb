<div class="card shadow-lg border-0 mt-4">
  <div class="card-header bg-primary text-white">
    <h5 class="mb-0">📝 Write a Review</h5>
  </div>

  <div class="card-body">
    <%= form_with(model: [@book, @review], local: true) do |form| %>
      
      <% if @review.errors.any? %>
        <div class="alert alert-danger">
          <h6><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h6>
          <ul class="mb-0">
            <% @review.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="mb-3">
        <%= form.label :rating, "Rating (1–5)", class: "form-label fw-semibold" %>
        <%= form.number_field :rating, class: "form-control", min: 1, max: 5, placeholder: "Enter a rating between 1 and 5" %>
      </div>

      <div class="mb-3">
        <%= form.label :comment, "Your Comment", class: "form-label fw-semibold" %>
        <%= form.text_area :comment, class: "form-control", rows: 4, placeholder: "Share your thoughts about the book..." %>
      </div>

      <div class="text-end">
        <%= form.submit "Submit Review", class: "btn btn-success px-4" %>
      </div>

    <% end %>
  </div>
</div>
